version: '3.8'

services:
  web_app:
    build: .
    container_name: archive_code_container
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always